(setq inhibit-startup-message t)
(scroll-bar-mode -1)
(tool-bar-mode -1)
(tooltip-mode -1)
(set-fringe-mode 10)

(global-display-line-numbers-mode 1)
(setq display-line-numbers-type 'relative)

(load-theme 'wombat)
;;; load custom theme (add-to-list 'custom-theme-load-path "~/.emacs.d/themes/")
